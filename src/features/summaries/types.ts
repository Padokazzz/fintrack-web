export type MonthlySummary = {
  month: number;
  year: number;
  totalIncome: number;
  totalExpense: number;
  finalBalance: number;
};

export type OverallBalance = {
  totalBalance: number;
  accountsCount: number;
};
